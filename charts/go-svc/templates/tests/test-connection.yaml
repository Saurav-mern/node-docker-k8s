apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-svc.fullname" . }}-test-connection"
  labels:
    {{- include "go-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
